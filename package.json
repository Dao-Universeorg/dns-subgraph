{
  "name": "dns-subgraph",
  "version": "1.0.0",
  "repository": "https://github.com/Dao-Universeorg/dns-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "auth": "graph auth --product hosted-service ",
    "deploy": "graph deploy daoalien/dns-subgraph --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/"
  },
  "devDependencies": {
    "@ensdomains/content-hash": "^2.5.3",
    "@graphprotocol/graph-cli": "^0.23.2",
    "@graphprotocol/graph-ts": "^0.23.1"
  }
}
